name: Refresh IBM i jobs
on:
  schedule:
    - cron: '17 5 * * *'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node scripts/refresh-jobs.mjs
      - name: Commit changed job status
        run: |
          git config user.name 'learn-ibmi job refresh'
          git config user.email 'actions@users.noreply.github.com'
          git add content/jobs.json
          git diff --cached --quiet || git commit -m 'Refresh IBM i job source status'
          git push
