{
  "sqlModules": [
    {"id":"sql-foundations","level":"Beginner","title":"Db2 for i foundations","summary":"Understand schemas, tables, rows, columns, keys, nulls, and the difference between a statement and a transaction.","points":["A table is a set of rows described by columns; a physical file can be used through SQL, but a logical file is not the same thing as a SQL view.","Use a qualified name such as MYLIB.CUSTOMER so a library-list change cannot silently select another object.","A primary key expresses identity. A unique constraint enforces a rule. An index supports access; it does not replace a business rule."],"code":"SELECT CUSTOMER_ID, NAME\n  FROM MYLIB.CUSTOMER\n WHERE STATUS = 'A'\n ORDER BY CUSTOMER_ID;","sources":[{"title":"IBM: Db2 for i SQL programming","url":"https://www.ibm.com/docs/en/i/7.5.0?topic=programming-sql"},{"title":"IBM: Db2 for i SQL reference","url":"https://www.ibm.com/docs/en/i/7.5.0?topic=reference-sql"}]},
    {"id":"sql-dml","level":"Beginner","title":"SELECT, INSERT, UPDATE, and DELETE","summary":"Write safe data-changing statements and explain the row set or predicate before executing them.","points":["SELECT should name the columns needed by the caller. Avoid SELECT * in stable interfaces because schema changes can change the result contract.","INSERT should name its target columns. UPDATE and DELETE need a deliberate WHERE predicate and a row-count check.","Use parameter markers from the application rather than concatenating user input into SQL text."],"code":"-- Preview the exact rows before changing them\nSELECT ORDER_ID, STATUS\n  FROM MYLIB.ORDERS\n WHERE CUSTOMER_ID = :customerId;\n\nUPDATE MYLIB.ORDERS\n   SET STATUS = :newStatus\n WHERE ORDER_ID = :orderId;","sources":[{"title":"IBM: INSERT statement","url":"https://www.ibm.com/docs/en/i/7.5.0?topic=statements-insert"},{"title":"IBM: UPDATE statement","url":"https://www.ibm.com/docs/en/i/7.5.0?topic=statements-update"},{"title":"IBM: DELETE statement","url":"https://www.ibm.com/docs/en/i/7.5.0?topic=statements-delete"}]},
    {"id":"sql-joins","level":"Intermediate","title":"Joins, grouping, and subqueries","summary":"Combine related tables while preserving the intended cardinality and handling missing related rows.","points":["INNER JOIN returns only matching rows. LEFT JOIN keeps the left row and produces nulls when the right side is absent.","Put filters on the correct side of an outer join; a WHERE predicate on the right table can turn a LEFT JOIN into an inner result.","GROUP BY creates one row per group. HAVING filters groups after aggregation; WHERE filters rows before aggregation."],"code":"SELECT c.CUSTOMER_ID, c.NAME, COUNT(o.ORDER_ID) AS ORDER_COUNT\n  FROM MYLIB.CUSTOMER AS c\n  LEFT JOIN MYLIB.ORDERS AS o\n    ON o.CUSTOMER_ID = c.CUSTOMER_ID\n GROUP BY c.CUSTOMER_ID, c.NAME\nHAVING COUNT(o.ORDER_ID) > 0;","sources":[{"title":"IBM: Joined tables","url":"https://www.ibm.com/docs/en/i/7.5.0?topic=queries-joined-tables"},{"title":"IBM: GROUP BY clause","url":"https://www.ibm.com/docs/en/i/7.5.0?topic=elements-group-by-clause"}]},
    {"id":"sql-views","level":"Intermediate","title":"Views and reusable query contracts","summary":"Use views to expose a stable, permission-aware shape without copying data.","points":["A view stores a query definition. It normally does not store a second copy of the table rows.","Use a view to hide joins, expose only approved columns, or give reporting consumers a stable name.","Document whether a view is intended for reading only and test how inserts or updates behave before allowing them."],"code":"CREATE VIEW MYLIB.ACTIVE_CUSTOMERS AS\n  SELECT CUSTOMER_ID, NAME, STATUS\n    FROM MYLIB.CUSTOMER\n   WHERE STATUS = 'A';\n\nSELECT * FROM MYLIB.ACTIVE_CUSTOMERS;","sources":[{"title":"IBM: CREATE VIEW statement","url":"https://www.ibm.com/docs/en/i/7.5.0?topic=statements-create-view"},{"title":"IBM: Views","url":"https://www.ibm.com/docs/en/i/7.5.0?topic=views"}]},
    {"id":"sql-variables-cursors","level":"Intermediate","title":"Variables, indicators, and cursors","summary":"Move values safely between SQL and RPG, and process multi-row results without stale data.","points":["A host variable is an RPG variable referenced by a colon in embedded SQL.","A nullable column needs a null indicator or a nullable host structure; do not confuse NULL with zero or blank.","A cursor has a lifecycle: DECLARE, OPEN, FETCH, handle end-of-data, then CLOSE. A singleton SELECT is not a substitute for a cursor when multiple rows are expected."],"code":"EXEC SQL\n  DECLARE orderCursor CURSOR FOR\n    SELECT ORDER_ID, TOTAL\n      FROM MYLIB.ORDERS\n     WHERE CUSTOMER_ID = :customerId;\n\nEXEC SQL OPEN orderCursor;\n// FETCH in a loop; leave when SQLSTATE = '02000'\nEXEC SQL CLOSE orderCursor;","sources":[{"title":"IBM: Using host variables in ILE RPG applications","url":"https://www.ibm.com/docs/en/i/7.5.0?topic=applications-using-host-variables"},{"title":"IBM: Cursors in ILE RPG applications","url":"https://www.ibm.com/docs/en/i/7.5.0?topic=applications-cursors"}]},
    {"id":"sql-advanced","level":"Advanced","title":"Transactions, isolation, and performance","summary":"Make changes atomic, understand locks, and measure access paths before changing indexes or SQL.","points":["Commitment control groups related changes into a unit that can commit or roll back together.","Isolation affects what a transaction can see and how it locks data. Choose it from the consistency requirement, not from habit.","When a statement slows down, capture the statement, row counts, access plan, waits, and timing. An index change without measurement can make another workload worse."],"code":"EXEC SQL\n  SET TRANSACTION ISOLATION LEVEL READ COMMITTED;\n\nEXEC SQL UPDATE MYLIB.ORDERS\n            SET STATUS = 'P'\n          WHERE ORDER_ID = :orderId;\n\n// Check SQLSTATE, then COMMIT or ROLLBACK as one unit","sources":[{"title":"IBM: Commitment control","url":"https://www.ibm.com/docs/en/i/7.5.0?topic=integrity-commitment-control"},{"title":"IBM: Isolation levels","url":"https://www.ibm.com/docs/en/i/7.5.0?topic=locks-isolation-levels"},{"title":"IBM: Db2 for i performance","url":"https://www.ibm.com/docs/en/i/7.5.0?topic=performance"}]}
  ],
  "fileOps": [
    {"opcode":"READ","meaning":"Read the next sequential record from the current file position.","example":"read CUSTOMER;\nif not %eof(CUSTOMER); // use the record","sql":"SELECT ... ORDER BY key"},
    {"opcode":"CHAIN","meaning":"Retrieve one record by key or relative record number.","example":"chain customerId CUSTOMER;\nif %found(CUSTOMER); // use the record","sql":"SELECT ... FROM CUSTOMER WHERE CUSTOMER_ID = :customerId"},
    {"opcode":"SETLL","meaning":"Position at the first record greater than or equal to a key; %EQUAL tells you whether the key exists.","example":"setll customerId CUSTOMER;\nif %equal(CUSTOMER); // exact key exists","sql":"SELECT ... WHERE CUSTOMER_ID >= :customerId ORDER BY CUSTOMER_ID FETCH FIRST 1 ROW ONLY"},
    {"opcode":"SETGT","meaning":"Position after a key so a following READP can retrieve the prior record.","example":"setgt customerId CUSTOMER;\nreadp CUSTOMER;","sql":"SELECT ... WHERE CUSTOMER_ID < :customerId ORDER BY CUSTOMER_ID DESC FETCH FIRST 1 ROW ONLY"},
    {"opcode":"READE","meaning":"Read the next record while its key remains equal to the search key.","example":"setll customerId ORDER;\nreade customerId ORDER;","sql":"SELECT ... WHERE CUSTOMER_ID = :customerId ORDER BY key"},
    {"opcode":"READP","meaning":"Read the previous record relative to the current file position.","example":"readp CUSTOMER;\nif not %eof(CUSTOMER);","sql":"SELECT ... WHERE key < :currentKey ORDER BY key DESC FETCH FIRST 1 ROW ONLY"},
    {"opcode":"READPE","meaning":"Read the previous record whose key equals the search key.","example":"readpe customerId ORDER;","sql":"SELECT ... WHERE CUSTOMER_ID = :customerId AND key < :currentKey ORDER BY key DESC FETCH FIRST 1 ROW ONLY"},
    {"opcode":"WRITE","meaning":"Create a new record in an output-capable file.","example":"eval orderId = nextId;\nwrite ORDER;","sql":"INSERT INTO MYLIB.ORDER (ORDER_ID, ...) VALUES (:orderId, ...)"},
    {"opcode":"UPDATE","meaning":"Modify the most recently read record that is eligible for update.","example":"chain orderId ORDER;\nif %found(ORDER); update ORDER;","sql":"UPDATE MYLIB.ORDER SET ... WHERE ORDER_ID = :orderId"},
    {"opcode":"DELETE","meaning":"Delete the current eligible record or a record found by key.","example":"chain orderId ORDER;\nif %found(ORDER); delete ORDER;","sql":"DELETE FROM MYLIB.ORDER WHERE ORDER_ID = :orderId"},
    {"opcode":"EXFMT","meaning":"Write a display format and wait for input in one operation.","example":"exfmt OrderScreen;","sql":"No direct SQL equivalent; use a UI boundary around the database operation"},
    {"opcode":"READC","meaning":"Read the next changed subfile record.","example":"readc SFLREC;\nif not %eof(); // validate changed row","sql":"No direct SQL equivalent; changed-row state belongs to the display program"},
    {"opcode":"OPEN / CLOSE","meaning":"Explicitly open or close a file when USROPN or resource ownership requires it.","example":"open CONFIG;\n// use it\nclose CONFIG;","sql":"Connection and cursor lifecycle are the closest SQL analogue"},
    {"opcode":"COMMIT / ROLBK","meaning":"Commit or roll back the current transaction when commitment control is active.","example":"commit;\n// or rollback on error","sql":"COMMIT; / ROLLBACK;"}
  ],
  "comparisons": [
    {"rpg":"CHAIN customerId CUSTOMER","sql":"SELECT * FROM MYLIB.CUSTOMER WHERE CUSTOMER_ID = :customerId","when":"One keyed lookup","note":"CHAIN returns a record buffer and %FOUND; SQL returns a result row and SQLSTATE. Both need a not-found branch."},
    {"rpg":"SETLL key MYFILE + %EQUAL","sql":"SELECT 1 FROM MYLIB.MYFILE WHERE KEY = :key FETCH FIRST 1 ROW ONLY","when":"Check whether a key exists","note":"SETLL can be a cheaper existence check than CHAIN in suitable RPG access paths. SQL still needs a deliberate index and plan."},
    {"rpg":"SETLL customerId ORDER; READE customerId ORDER","sql":"SELECT * FROM MYLIB.ORDER WHERE CUSTOMER_ID = :customerId ORDER BY ORDER_ID","when":"Read all equal-key rows","note":"READE stops at the equal-key boundary; SQL expresses the whole row set. Always define ordering when the application depends on it."},
    {"rpg":"READ MYFILE in a loop","sql":"SELECT ... FROM MYLIB.MYFILE ORDER BY key","when":"Sequential processing","note":"RPG uses a file cursor and EOF status; SQL uses a cursor and SQLSTATE 02000. Do not process stale host variables after end-of-data."},
    {"rpg":"WRITE MYFILE","sql":"INSERT INTO MYLIB.MYFILE (...) VALUES (...)","when":"Create a row","note":"Both can fail on duplicate keys or constraints. Handle the failure and preserve the diagnostic."},
    {"rpg":"UPDATE MYFILE","sql":"UPDATE MYLIB.MYFILE SET ... WHERE KEY = :key","when":"Change a row","note":"RPG update depends on the current record and lock state; SQL should make the predicate and optimistic version check explicit."},
    {"rpg":"DELETE MYFILE","sql":"DELETE FROM MYLIB.MYFILE WHERE KEY = :key","when":"Remove a row","note":"Preview the predicate and check affected-row count. A missing key is different from a successful delete."},
    {"rpg":"SETGT key; READP MYFILE","sql":"SELECT ... WHERE key < :key ORDER BY key DESC FETCH FIRST 1 ROW ONLY","when":"Find the previous key","note":"The SQL ordering and strict less-than boundary must match the RPG cursor intent."}
  ],
  "errors": [
    {"name":"SQLSTATE 02000 / %EOF","meaning":"No row or end of data","action":"Branch before using host variables or record fields; do not process the previous row again."},
    {"name":"SQLSTATE 23505 / duplicate key","meaning":"A unique rule rejected the insert or update","action":"Decide whether this is an expected business conflict or an application defect; do not blindly retry."},
    {"name":"SQL7008","meaning":"The object is not valid for the requested SQL operation; the reason detail matters","action":"Read the complete diagnostic, then inspect journaling, commitment control, object type, and authority for that reason."},
    {"name":"RPG status 1218 / record in use","meaning":"A record is locked by another job","action":"Inspect the holder, use a bounded retry only when safe, and re-read before updating."},
    {"name":"RPG %FOUND false or %EOF true","meaning":"The keyed read did not find a row or the scan reached its boundary","action":"Handle the branch explicitly and reset/reposition before trying another scan."},
    {"name":"CPF4131 / level check","meaning":"The compiled file format does not match the file being opened","action":"Identify the resolved object and format change, then rebuild or deploy the compatible pair in a controlled sequence."}
  ],
  "sources":[
    {"title":"IBM: ILE RPG operation codes","url":"https://www.ibm.com/docs/en/i/7.5.0?topic=oc-operation-codes"},
    {"title":"IBM: Data management and ILE RPG I/O operations","url":"https://www.ibm.com/docs/en/i/7.6.0?topic=files-data-management-operations-ile-rpg-io-operations"},
    {"title":"IBM: Keys for file operations","url":"https://www.ibm.com/docs/en/i/7.5.0?topic=operations-keys-file"},
    {"title":"MainframesTechHelp: Db2 tutorial index","url":"https://www.mainframestechhelp.com/tutorials/db2/"}
  ]
}
